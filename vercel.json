{
  "framework": null,
  "buildCommand": "pnpm install && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/kyrbirt run build && cp -r artifacts/kyrbirt/dist dist",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/handler" }
  ],
  "functions": {
    "api/handler.mjs": {
      "memory": 512,
      "maxDuration": 30
    }
  }
}
