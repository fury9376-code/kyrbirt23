{
  "buildCommand": "pnpm install && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/kyrbirt run build",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/handler" }],
  "functions": {
    "api/handler.mjs": {
      "memory": 512,
      "maxDuration": 30
    }
  }
}
