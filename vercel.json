{
  "buildCommand": "pnpm install && cd lib/db && pnpm run push && cd ../.. && pnpm --filter @workspace/kyrbirt run build",
  "outputDirectory": "artifacts/kyrbirt/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/handler" }
  ],
  "functions": {
    "api/handler.ts": {
      "memory": 512,
      "maxDuration": 30
    }
  }
}
